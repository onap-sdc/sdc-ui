/*
import { async, ComponentFixture, TestBed } from '@angular/core/testing';
import { TestTooltipComponent } from './test.tooltip.component';

describe('Tooltip directive', () => {
    let component: TestTooltipComponent;
    let fixture: ComponentFixture<TestTooltipComponent>;

    beforeEach(async(() => {
        TestBed.configureTestingModule({
            declarations: [ TestTooltipComponent ]
        })
            .compileComponents();
    }));

    beforeEach(() => {
        fixture = TestBed.createComponent(TestTooltipComponent);
        component = fixture.componentInstance;
        fixture.detectChanges();
        //fixture.debugElement.query(By.id('txtTest'));
    });

    it('component should be created', () => {
        expect(component).toBeTruthy();
    });

    // it('when no input params given should be without attributes (default)', () => {
    // });
    //
    // // left placement
    // it('when placement parameter = "left" given should be tooltip in left placement', () => {
    // });
    //
    // it('when placement parameter = "left" and do not place in the left side given should be tooltip in right placement', () => {
    // });
    //
    // it('when placement parameter = "left" and do not place in the left and right sides given should be tooltip in top placement', () => {
    // });
    //
    // it('when placement parameter = "left" and do not place in the left, right and top sides given should be tooltip in bottom placement', () => {
    // });
    //
    // // right placement
    // it('when placement parameter = "right" given should be tooltip in right placement', () => {
    // });
    //
    // it('when placement parameter = "right" and do not place in the right side given should be tooltip in left placement', () => {
    // });
    //
    // it('when placement parameter = "right" and do not place in the left and right sides given should be tooltip in top placement', () => {
    // });
    //
    // it('when placement parameter = "right" and do not place in the left, right and top sides given should be tooltip in bottom placement', () => {
    // });
    //
    // // top placement
    // it('when placement parameter = "top" given should be tooltip in top placement', () => {
    // });
    //
    // it('when placement parameter = "top" and do not place in the top side given should be tooltip in bottom placement', () => {
    // });
    //
    // it('when placement parameter = "top" and do not place in the top and bottom sides given should be tooltip in left placement', () => {
    // });
    //
    // it('when placement parameter = "top" and do not place in the top, bottom and left sides given should be tooltip in right placement', () => {
    // });
    //
    // // bottom placement
    // it('when placement parameter = "bottom" given should be tooltip in bottom placement', () => {
    // });
    //
    // it('when placement parameter = "bottom" and do not place in the bottom side given should be tooltip in top placement', () => {
    // });
    //
    // it('when placement parameter = "bottom" and do not place in the top and bottom sides given should be tooltip in left placement', () => {
    // });
    //
    // it('when placement parameter = "bottom" and do not place in the top, bottom and left sides given should be tooltip in right placement', () => {
    // });
    //
    // // custom css class
    //
    // // is disabled
    // it('when disabled parameter is true given should be no tooltip', () => {
    // });
});
*/
